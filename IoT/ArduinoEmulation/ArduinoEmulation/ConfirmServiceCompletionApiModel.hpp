#include <string>
#include <ctime>

using namespace std;

namespace ArduinoEmulation {

    struct ConfirmServiceCompletionApiModel {
        double latitude;
        double longitude;
        string dateTime;
    };
}