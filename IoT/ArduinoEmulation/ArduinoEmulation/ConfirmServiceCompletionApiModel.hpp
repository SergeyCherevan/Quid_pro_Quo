#include <string>
#include <ctime>

namespace ArduinoEmulation {

    using namespace std;

    struct ConfirmServiceCompletionApiModel {
        double latitude;
        double longitude;
        string dateTime;
    };
}